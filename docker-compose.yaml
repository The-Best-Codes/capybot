services:
  app:
    # exclude_from_hc: true
    build:
      context: .
    volumes:
      - .:/app
  web:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    # exclude_from_hc: true
    image: nginx:alpine
    ports:
      - "3000:80"
