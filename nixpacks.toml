[phases.setup]
pkgs = ["nodejs_18", "npm-9_x", "bun"]
aptPkgs = ["python3", "g++", "make"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bunx --bun discraft start --runner node"
