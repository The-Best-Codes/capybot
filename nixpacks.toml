[phases.setup]
nixpkgsArchive = "a8cae9466f5b9e3564cf8058d7d9a7496af53f01"
pkgs = ["...", "nodejs_23"]
aptPkgs = ["python3", "g++", "make"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun run start"
